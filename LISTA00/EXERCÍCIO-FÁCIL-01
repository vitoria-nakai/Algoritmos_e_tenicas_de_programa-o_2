/*Crie um programa que defina uma struct para representar um aluno. A estrutura deve conter os seguintes campos:
nome (string)
idade (inteiro)
media (ponto flutuante)
Depois de definir a struct, declare uma variável do tipo da estrutura, preencha seus campos com dados e, em seguida, exiba os dados na tela.
*/

#include<stdio.h>
#include<stdlib.h>

struct aluno{
    char nome[50];
    int idade;
    float média;
};

int main(){
    struct aluno Vitoria;
    printf("\nDigite o nome do aluno(a): ");
    scanf("%s", Vitoria.nome);
    printf("\nDigite a idade do aluno(a):");
    scanf("%d", &Vitoria.idade);
    printf("\nDigite a nota média do aluno(a): ");
    scanf("%f", &Vitoria.média);
    printf("\nRevise os dados.\n Nome: %s\n Idade: %d\n Média: %f", Vitoria.nome, Vitoria.idade, Vitoria.média);
    getchar();
    return 0;
}
