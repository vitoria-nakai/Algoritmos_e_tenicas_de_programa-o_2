/*
Uma função recursiva para inverter uma string. A função deve modificar a string original e não criar uma nova string. 
Considere que a função recebe como parâmetros a string, o índice de início e o índice de fim (ou seja, a posição no 
array do primeiro e do último caracter da string). Faça com que a função main escreva a string antes e depois da chamada da função recursiva.
*/

#include<stdio.h>
#include<stdlib.h>
#include<string.h>

char * inverter(char string[], int inicio, int fim){
    char med;
    char med2;
    
    if(inicio > fim) return string;
    else {
        med = string[inicio];
        med2 = string[fim];
        string[inicio] = med2;
        string[fim] = med;
        return inverter(string, inicio + 1, fim - 1);
    }
}

int main(){
    char str[100];
    int start = 0, end;
    
    printf("Digite uma string: ");
    fgets(str, sizeof(str), stdin);
    str[strcspn(str, "\n")] = '\0';
    
    end = strlen(str) - 1;
    
    printf("Sua string original é %s\n", str);
    
    printf("Sua string invertida é %s\n", inverter(str, start, end));
    
    return 0;
}
